These dumps were obtained from `dconf dump` given specific paths to keybinding
settings. They can be imported via `dconf load <PATH>` where path is the
filename with `.` replaced with `/` and a `/` at the end.

